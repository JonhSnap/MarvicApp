import React from "react";

const ProfileDetails = ({ user, label }) => {
  return (
    <div>
      <label htmlFor="">{label}</label>
      <input type="text" value={value} />
    </div>
  );
};

export default ProfileDetails;
